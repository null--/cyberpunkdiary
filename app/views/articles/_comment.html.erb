<div>
  <p><%= User.find(comment.user_id).nickname %> said:</p>
  <p class="comment"><%= comment.body %></p>
  <p><i>Posted/Updated: <%= distance_of_time_in_words(DateTime.now, comment.created_at) %> later</i></p>
  <br>
</div>

